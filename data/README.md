# Data

The AG News dataset is downloaded automatically by notebook 01 using Hugging Face `datasets`.

Raw data is intentionally not committed to GitHub. Notebook 01 creates the generated intermediate file `data/ag_news_preprocessed.pkl`, which is ignored by Git.
